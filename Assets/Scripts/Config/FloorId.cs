namespace Lilo.Config
{
    /// <summary>Canonical floor identifier shared by every spec that needs to key data per floor.</summary>
    public enum FloorId
    {
        Floor52,
        Floor51,
        Floor50,
    }
}
